# Feature registry (media-manager)

**Purpose:** Single map of product behavior, implementation locations, and known gaps.  
**Audience:** Humans and agents working in this repo.  
**Companion:** User-facing setup, data layout, and how to run the built app live in [`README.md`](../README.md). The deferred-work backlog lives in [`FUTURE_CHANGES.md`](FUTURE_CHANGES.md) (the old `tasks.md` has been consolidated there).

**Last reviewed:** 2026-06-22

---

## How to use this document

1. Before changing behavior, find the relevant **feature row** and **primary files**.
2. After shipping a change, update this file in the **same PR or commit** (feature table, API index, media-kind notes, or residual notes).
3. If you add routes, API handlers, or major UI flows, add or extend rows here—do not rely only on README.

---

## Files, classes, and storage

**File-first model.** Files (blobs) are the primary entity; a **class** is a schema + opt-in per-blob metadata. The full design is [`docs/FILE_FIRST_CLASSES.md`](FILE_FIRST_CLASSES.md). On disk:

```
<root>/media/{ manifest.json, files/, classes/<id>.json, settings.json }   # the hub
<root>/<jsonType>/{ settings.json, data.json }                             # `json` types (incl. globals)
```

| Concept     | Meaning                                                                              | Storage                                                   | Repo                                              | UI                                                                                     |
| ----------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Blob        | A file in `media/files/`, identity = manifest `file_id` (UUID)                       | [`manifest.ts`](../src/lib/storage/manifest.ts)           | [`classRepo.ts`](../src/lib/storage/classRepo.ts) | All Files hub ([`files/+page.svelte`](../src/routes/files/+page.svelte))               |
| Class       | Schema + opt-in metadata keyed by `file_id` (any file type; empty schema = pure tag) | `media/classes/<id>.json` = `{ schema, config, records }` | [`classRepo.ts`](../src/lib/storage/classRepo.ts) | Hub sidebar + [`FileEditorPanel.svelte`](../src/lib/components/FileEditorPanel.svelte) |
| `json` type | Records only, no file attachment (`records/` reorg deferred — Item 18)               | `<root>/<typeId>/data.json`                               | [`jsonRepo.ts`](../src/lib/storage/jsonRepo.ts)   | Records Explorer ([`media/+page.svelte`](../src/routes/media/+page.svelte))            |

**Membership is opt-in and binary (by design):** a blob is a **member** of a class iff that class file has a record keyed by its id; **unclassified** = empty `classes[]`. There is **no** linked/unlinked/excluded tri-state, no `excludedFiles`, and **no per-extension gating** — any blob can belong to any class. The manifest's `classes[]` is a **derived cache**; class files are the source of truth. Consequences (all expected):

- **Add/remove to class** ([`addMembers`/`removeMembers`](../src/lib/storage/classRepo.ts)) writes the class file and updates `manifest.classes[]` atomically. Bulk add creates empty member records (fields filled inline later).
- **mtime-gated resync:** on list, if any class file is newer than the manifest, the index is rebuilt from class files ([`applyMembershipIndex`](../src/lib/storage/manifest.ts)) — catches hand edits.
- **Lazy heal:** every list reconciles the manifest against disk ([`reconcile`](../src/lib/storage/manifest.ts)) — new blobs get an id, vanished blobs are flagged `missing: true` (kept). Response carries `healed: { added, missing }`.
- **Rename** is **O(1)** ([`renameBlobById`](../src/lib/storage/classRepo.ts) → [`manifest.renameFileId`](../src/lib/storage/manifest.ts)) — disk + one manifest entry, no fan-out.
- **Delete from disk** drops the manifest entry and strips the member record from **every** class ([`deleteFromDiskById`](../src/lib/storage/classRepo.ts)). **Delete class** removes the file + strips its id from `classes[]`; blobs untouched.
- **Locking:** manifest mutations are atomic under one `.lock`, taken **before any class lock**.

**Filenames:** blobs use [`assertSafeBasename`](../src/lib/storage/filenames.ts) (any extension). Intrinsic image dims are backfilled into manifest entries on listing.

**Root discovery:** `MEDIA_MANAGER_ROOT` → [`getRootDir()`](../src/lib/storage/paths.ts). `listClassIds()` scans `media/classes/`; `listMediaTypeIds()` scans the root for `json` types (skipping `media/`).

**Reserved type ids:** only `globals` ([`RESERVED_TYPE_IDS`](../src/lib/storage/mediaTypes.ts), auto-created/healed by `ensureGlobalsGroupExists()`) — protected from create/rename/delete. The old `files` blob_store type is gone (`media/` is the hub). No reserved classes.

**Missing files:** a `file`-field value (or a class membership) referencing a blob absent from disk is surfaced globally via `GET /api/files/missing` ([`listMissingFileReferences`](../src/lib/storage/classRepo.ts)) and the hub's warning banner; class list items still carry `missing_file_fields: string[]` and records carry `_missing_files: { field → expected filename }`.

---

## Feature catalog

Status legend: **stable** (works end-to-end for the happy path) · **partial** (implemented but incomplete or inconsistent) · **experimental** (may change)

| Feature                      | Expected behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status | Primary files                                                                                                                                                                                                                                                                                                                                                                                                                                       | API / notes                                                                                                                |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Dashboard (home)             | `/`: a **launcher** for the three peer sub-apps — **Files** (`/files`, count = files · classes), **Records** (`/media`, count = `json` types excl. globals), **Globals** (`/globals`, count = user fields) — as three entry `Card`s, plus the global `Settings` (the single unified `SettingsButton` dialog — grid size + navigation prefs + appearance). Deliberately does **not** re-list individual classes/types or offer create actions — per-entity browsing + creation live inside each sub-app's rail (`/files` `+ New class`, `/media` `+ New`), so the home stays a thin router. Globals is a top-level peer route (`/globals`), no longer nested under Records                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | stable | [`+page.svelte`](../src/routes/+page.svelte), [`SettingsButton.svelte`](../src/lib/components/SettingsButton.svelte)                                                                                                                                                                                                                                                                                                                                | [`files.ts`](../src/lib/api/files.ts), [`client.ts`](../src/lib/api/client.ts) (`apiListMediaTypes`/`apiGetGlobalsRecord`) |
| All Files hub                | `/files`: shared **collapsible `EntityRail`** class filter list (OR/AND + Unclassified) w/ per-class `⋮` menu (**unified `EntitySettingsDialog`** — rename/title-by/group-by/schema/delete; the active solo class's ⋮ is mirrored in the content header so settings stay reachable when the rail is collapsed), **search (shared `SearchBox`) + a search-field picker (`SearchFieldSelect`: scopes the search to a solo class's field, a `Class: field` across an intersection, or else just filename — server-side via `?searchField`) + match-any/all toggle in the sidebar**, shared `DataGrid` of tiles w/ class chips, a header **Select** toggle that enters multiselect (tiles toggle-select on click, marked by a ring; becomes **Clear**) driving bulk add/remove/delete, missing-files banner. **Grid size lives in the top toolbar in every view** and is a single global app setting (`media/settings.json` via `GET`/`POST /api/settings`) shared by every view and `json` grid — no per-class or per-type override. Group-by is context-aware: solo class ⇒ group by that class's fields (server-side `group_by_value`); 0 classes / "any of" ⇒ optional **Group-by-class** (flat by default) keying each file by its exact class set ("Images + Documents", member-less ⇒ "Unclassified"); "all of" (multi-class intersection) ⇒ group by a `Class: field` across the intersected classes (`GET /api/files?groupByClass&groupByField`). `?class=<id>` or one class selected ⇒ that class's catalog view. A **breadcrumb** bar (`Home › Files › <solo class \| All Files>`, `Breadcrumbs.svelte`) sits atop the content; the **open file is deep-linked** via `?file=<id>` (alongside `?class=`) so reload/share/back-forward reopen the same editor (`replaceState`, restored on mount)                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | stable | [`files/+page.svelte`](../src/routes/files/+page.svelte), [`EntityRail.svelte`](../src/lib/components/rail/EntityRail.svelte), [`EntitySettingsDialog.svelte`](../src/lib/components/entity-settings/EntitySettingsDialog.svelte), [`DataGrid.svelte`](../src/lib/components/data-grid/DataGrid.svelte), [`FileEditorPanel.svelte`](../src/lib/components/FileEditorPanel.svelte), [`Breadcrumbs.svelte`](../src/lib/components/Breadcrumbs.svelte) | [`files.ts`](../src/lib/api/files.ts)                                                                                      |
| Per-file editor              | One collapsible section per class the blob belongs to (schema-driven fields via shared `FieldInput`), rename, EXIF `MetadataButton` (images only), add/remove-to-class, **prev/next chevrons + ←/→ keys to walk the filtered grid order**, **debounced autosave** (~600ms; flush on field blur/Enter, prev-next, file switch, close, `beforeunload`; "Saving…/✓ Saved" status; **no explicit per-section Save button**) via the shared `debouncedAutosave` helper. A field autosave fires **`onSaved`** (grid tile unchanged ⇒ host does **NOT** refetch — refetching reassigned the `file` prop and reloaded sections mid-typing, dropping keystrokes); rename / add-remove class / metadata fire **`onStructureChanged`** (host reloads). Built on the shared `EditorPanelShell` (same chrome + save behavior as the Records detail pane). The panel only mounts when a file is selected (no reserved space)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | stable | [`FileEditorPanel.svelte`](../src/lib/components/FileEditorPanel.svelte), [`EditorPanelShell.svelte`](../src/lib/components/EditorPanelShell.svelte), [`FieldInput.svelte`](../src/lib/components/FieldInput.svelte)                                                                                                                                                                                                                                | `GET /api/files/[id]/classes`, `PATCH /api/classes/[id]/records/[fileId]`                                                  |
| Records Explorer (3-pane)    | `/media` (the records sub-app's home): a persistent three-pane layout — a **collapsible type rail** (`RecordsRail` on the shared `EntityRail` shell: every `json` type, generic glyph, per-type `⋮` menu → **unified `EntitySettingsDialog`** (rename + **Title by** + schema + delete), an inline **`+ New`** next to the "Record types" label, rail-hosted **search (`SearchBox`) + search-field picker (`SearchFieldSelect`, the active type's fields; server-side via `?searchQuery&searchField`)**, Home + **App settings** footer; cross-cutting navigator, **records-only — no merged "All Records" view**; reserved `globals` has no ⋮), the **record list column** (`RecordListColumn`: name-forward scrollable list (records-native — deliberately not the tile `DataGrid`), **Group by** (ephemeral), a content-header `⋮` mirroring the active type's settings (reachable when the rail is collapsed), header **Select** toggle → multiselect + bulk **Set field… / Delete** via `RecordBulkActions`, missing-file ⚠ badge per row). **Title-by is no longer a header control** — it's a persisted per-type setting (`settings.json` `displayField`) edited in the ⋮ → Settings dialog, with an optional **Subtitle** field (`settings.json` `subtitleField`) rendered as a muted secondary line per row (group values stay only in the group header). Both resolve server-side into `title_value` / `subtitle_value`; the client trusts `title_value` first so a present `name` field no longer shadows the chosen title. There is also an inline **autosaving detail pane** (`RecordDetailPane`, see below). A **breadcrumb** bar (`Home › Records › <type>`, `Breadcrumbs.svelte`) sits atop the list column. Active type held in `?type=<id>` and the **open record is deep-linked** via `?record=<id>` (`replaceState`, restored on mount) so reload/share/back-forward reopen the same detail pane. `/media/[typeId]` is a **307 redirect** into the Explorer with that type preselected. `globals` is **no longer a record type here** — it moved to its own `/globals` sub-app, so it's filtered out of the rail (`fetchTypes`) and a stale `?type=globals` **redirects** to `/globals`. Rail-collapsed state is a persisted global setting (`railCollapsed`). | stable | [`media/+page.svelte`](../src/routes/media/+page.svelte), [`RecordsRail.svelte`](../src/lib/components/records/RecordsRail.svelte), [`RecordListColumn.svelte`](../src/lib/components/records/RecordListColumn.svelte), [`RecordBulkActions.svelte`](../src/lib/components/RecordBulkActions.svelte), [`recordDisplay.ts`](../src/lib/core/recordDisplay.ts)                                                                                        | `*ForType` helpers in [`client.ts`](../src/lib/api/client.ts); `?titleField=` / `?subtitleField=` on records `list`        |
| Per-record editor (autosave) | Inline detail pane of schema-driven fields (shared `FieldInput`); **autosaves** (debounced ~600ms + flush on field blur / prev-next / record switch / close / `beforeunload`) with a "Saving…/✓ Saved" status hint — **no explicit Save button** (Delete kept) — via the shared `debouncedAutosave` helper. On save it calls **`onchanged(updated)`** so the host **patches that one list row in place** (reprojecting it with `projectRecordRow`, the same projection `jsonRepo.listRecords` uses) — **no list refetch**, which previously flashed the page and dropped in-flight keystrokes. Composes the shared **`EditorPanelShell`** (prev/next chevrons + ←/→ keys, flush first) and only mounts when a record is selected (no reserved 440px placeholder). Display title resolves via `recordDisplay.ts` (name → chosen `titleField` → group value → short id). (Files + Globals share the same `debouncedAutosave` + no-refetch policy — see Per-file editor / Globals.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | stable | [`RecordDetailPane.svelte`](../src/lib/components/records/RecordDetailPane.svelte), [`FieldInput.svelte`](../src/lib/components/FieldInput.svelte), [`recordDisplay.ts`](../src/lib/core/recordDisplay.ts)                                                                                                                                                                                                                                          | `GET`/`POST /api/media-types/[typeId]/records/by-id/[id]`                                                                  |
| Schema editor                | Add/rename/delete fields (all field types, dropdown options + multiselect, list item types); protected keys. One shared body (`SchemaEditorBody`) driven by an injected adapter; it is now hosted in the **Fields tab** of the unified `EntitySettingsDialog` for both classes and `json` types (the standalone `ClassSchemaDialog` + `SchemaEditorButton` were retired). Extras (Export/Import/Clone/Validate&Repair) are **not surfaced** for now (endpoints intact; re-add via an optional slot)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | stable | [`SchemaEditorBody.svelte`](../src/lib/components/schema-editor/SchemaEditorBody.svelte), [`EntitySettingsDialog.svelte`](../src/lib/components/entity-settings/EntitySettingsDialog.svelte), [`adapters.ts`](../src/lib/components/entity-settings/adapters.ts)                                                                                                                                                                                    | `/api/classes/[id]/schema` (classes), `/api/media-types/[typeId]/schema` (json)                                            |
| String field editing         | Every `string`-field value renders as an autogrow `<textarea>` (starts one row, grows to fit) in all three editor panes; **Enter** blurs + saves, **Shift+Enter** inserts a newline. No per-field toggle/`long` flag — all strings are multiline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | stable | [`autogrow.ts`](../src/lib/actions/autogrow.ts), [`FieldInput.svelte`](../src/lib/components/FieldInput.svelte), [`RecordDetailPane.svelte`](../src/lib/components/records/RecordDetailPane.svelte), [`GlobalsEditorPane.svelte`](../src/lib/components/GlobalsEditorPane.svelte)                                                                                                                                                                   | —                                                                                                                          |
| URL field editing            | `url`-type values (`{display_name, url}`) are **always-editable in place** in all three editor panes via the shared `FieldInput`: a single `url` field shows the display-name + URL inputs on **one row**; a `list`-of-`url` field renders **one inline-editable row per item** (display-name + URL inputs + per-row delete) plus a blank "add" row at the bottom — existing items are edited by typing, not delete-and-re-add. Each filled URL gets an **external-link icon button** (opens in a new tab; `https://` assumed for bare hosts; disabled + tooltip when empty) with a shadcn `Tooltip`. Non-url `list` items (string/number) keep the compact add/delete chip row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | stable | [`FieldInput.svelte`](../src/lib/components/FieldInput.svelte)                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                          |
| Filters                      | Multi-clause filters + legacy query/field/empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | stable | [`filters.ts`](../src/lib/core/filters.ts), repos                                                                                                                                                                                                                                                                                                                                                                                                   | `filters` query on list endpoints                                                                                          |
| Upload                       | Per-blob into `media/files/` (any type); overwrite / auto-rename; HEIC→JPEG                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | stable | [`files/+page.svelte`](../src/routes/files/+page.svelte), [`files/upload/+server.ts`](../src/routes/api/files/upload/+server.ts)                                                                                                                                                                                                                                                                                                                    | `POST /api/files/upload`                                                                                                   |
| Add / remove to class        | Opt-in membership: bulk add creates empty member records; remove drops them; both update the derived index                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | stable | [`files/+page.svelte`](../src/routes/files/+page.svelte), [`classRepo.ts`](../src/lib/storage/classRepo.ts)                                                                                                                                                                                                                                                                                                                                         | `POST/DELETE /api/classes/[id]/members`                                                                                    |
| Delete from disk             | Removes the blob + manifest entry; strips its member record from every class (confirmed via `AlertDialog`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | stable | [`files/+page.svelte`](../src/routes/files/+page.svelte)                                                                                                                                                                                                                                                                                                                                                                                            | `POST /api/files/delete` (`{ ids }`)                                                                                       |
| Rename file                  | O(1): renames the blob on disk + the single manifest entry; no fan-out                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | stable | [`FileEditorPanel.svelte`](../src/lib/components/FileEditorPanel.svelte), [`manifest.ts`](../src/lib/storage/manifest.ts)                                                                                                                                                                                                                                                                                                                           | `POST /api/files/[id]/rename`                                                                                              |
| File metadata                | Read / strip EXIF (all or GPS), rename + fix-extension, per-blob — the rich `MetadataButton` dialog, shown in the per-file editor for image files only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | stable | [`MetadataButton.svelte`](../src/lib/components/MetadataButton.svelte), [`FileEditorPanel.svelte`](../src/lib/components/FileEditorPanel.svelte)                                                                                                                                                                                                                                                                                                    | `/api/files/[id]/metadata`, `.../strip`, `POST /api/files/[id]/rename`                                                     |
| Stats popup                  | Record counts + last updated (`json` types)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | stable | —                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `GET /api/media-types/[typeId]/stats`                                                                                      |
| File field picker            | `file`-type schema fields select a blob (stores its `file_id`); the shared `FieldInput` renders a thumbnail+search `FilePicker` dialog (open-in-new-tab + missing flag) for every editor. `FilePicker` lists blobs from `/api/files` (the old `blob_store`-discovery path is gone)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | stable | [`FieldInput.svelte`](../src/lib/components/FieldInput.svelte), [`FilePicker.svelte`](../src/lib/components/FilePicker.svelte)                                                                                                                                                                                                                                                                                                                      | `GET /api/files`; bytes via `/api/files/[id]/blob`                                                                         |
| Missing-file warning         | Global banner + drill-down for any `file`-field/membership referencing an absent blob; class list items carry `missing_file_fields`, records carry `_missing_files`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | stable | [`files/+page.svelte`](../src/routes/files/+page.svelte), [`classRepo.ts`](../src/lib/storage/classRepo.ts) (`listMissingFileReferences`), [`manifest.ts`](../src/lib/storage/manifest.ts)                                                                                                                                                                                                                                                          | `GET /api/files/missing`                                                                                                   |
| Class management             | + New class (dashboard/hub; id = slug, fixed; renamable displayName); per-class `⋮` menu → unified `EntitySettingsDialog` (General: rename + display field + group-by · Fields: schema · Danger: delete, spares blobs); empty schema = pure tag. Shared with the Records type settings via the same dialog + adapters. The **General tab autosaves** (shared `debouncedAutosave`, ~600ms + flush on close; no Save button — Saving…/✓ Saved status; never autosaves a blank name) for both classes and record types                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | stable | [`files/+page.svelte`](../src/routes/files/+page.svelte), [`EntitySettingsDialog.svelte`](../src/lib/components/entity-settings/EntitySettingsDialog.svelte), [`adapters.ts`](../src/lib/components/entity-settings/adapters.ts), [`classRepo.ts`](../src/lib/storage/classRepo.ts)                                                                                                                                                                 | `/api/classes`, `/api/classes/[id]`, `/api/classes/[id]/schema`                                                            |
| Globals field types          | Full parity with json records: every field type (`string`/`number`/`boolean`/`dropdown` w/ options + multiselect/`list` w/ item types/`url`/`file`) plus the file picker and missing-file indicators. Field structure is edited in the per-field `⋮` `FieldMenu` (type/options/item-type/rename), not inline. Persists across reloads via reserved `__field_kinds` (key→type), `__field_meta` (key→`{options,multiselect,itemType}`), and `__layout` (sectioning) hints; the server derives a synthetic schema from `__field_kinds` only (`globalsSyntheticSchema`) so url-normalization + `_missing_files` apply (`__field_meta`/`__layout` are ignored by schema logic)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | stable | [`GlobalsEditorPane.svelte`](../src/lib/components/GlobalsEditorPane.svelte), [`fieldKeys.ts`](../src/lib/core/fieldKeys.ts), [`jsonRepo.ts`](../src/lib/storage/jsonRepo.ts)                                                                                                                                                                                                                                                                       | Hints stored as JSON-encoded strings in the singleton record; hidden from the field list                                   |
| Globals singleton            | Exactly one app-wide JSON object; free-form fields; no schema editing; protected from rename/delete. **Hosted at its own top-level `/globals` route** (`globals/+page.svelte`) — a peer sub-app of Files/Records, wrapping the editor in the shared `EntityRail` shell (sub-app switcher header + Home/Settings footer); the storage layer is unchanged (still a reserved `json` singleton on disk). Editor is a **sub-app-flavored sectioned property table** (`GlobalsEditorPane` + `globals/` child components): clean `key → value` rows grouped into **named collapsible sections** (`GlobalsSection`), a per-field **`⋮` `FieldMenu`** popover (type / options / item-type / rename / move-to-section / delete — same popover backs **+ Add field**), native HTML5 **drag** to reorder/move fields & sections (`⋮ → Move` fallbacks), a field **filter**, and a thin **`⚙` settings** dialog (`GlobalsSettingsDialog`: field-sort manual↔A→Z, default new-field section, whole-record JSON export/import). Sectioning/order/collapse/sort persist in reserved **`__layout`** (`core/globalsLayout.ts` — decode + load-time **reconcile** against actual keys, mirroring the manifest lazy-heal; pre-`__layout` records render as one `General` section). **Autosaves** via the shared `debouncedAutosave` (no Save button — Saving…/✓ Saved status; flush on Enter/`beforeunload`/unmount); advances its saved baseline **in place** (no reload after save, so typing never resets)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | stable | [`mediaTypes.ts`](../src/lib/storage/mediaTypes.ts), [`jsonRepo.ts`](../src/lib/storage/jsonRepo.ts), [`GlobalsEditorPane.svelte`](../src/lib/components/GlobalsEditorPane.svelte), [`globalsLayout.ts`](../src/lib/core/globalsLayout.ts), [`globals/`](../src/lib/components/globals/)                                                                                                                                                            | `GET/POST /api/media-types/globals/record`, protected behavior in `/api/media-types/[typeId]` + `/schema`                  |
| Sub-app switcher             | The rail header of all three sub-apps is a **dropdown** (`SubAppSwitcher` on `DropdownMenu`) that hops between **Files** (`/files`), **Records** (`/media`), **Globals** (`/globals`) — current item checkmarked — plus a Home link. Replaces the old static rail title; `EntityRail` takes a `current` prop and renders an icon-only trigger when collapsed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | stable | [`SubAppSwitcher.svelte`](../src/lib/components/rail/SubAppSwitcher.svelte), [`EntityRail.svelte`](../src/lib/components/rail/EntityRail.svelte)                                                                                                                                                                                                                                                                                                    | — (client-only nav via `goto`)                                                                                             |
| Command palette (⌘K)         | Global **⌘K / Ctrl-K** quick switcher (`CommandPalette` on the shadcn `Command` primitive, mounted once in `+layout.svelte`). Fuzzy-jump to a sub-app, a **class** (`/files?class=`), a **record type** (`/media?type=`), or Home. Entity lists are fetched lazily on first open. Navigation-only for now (searching individual files/records deferred).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | stable | [`CommandPalette.svelte`](../src/lib/components/CommandPalette.svelte), [`+layout.svelte`](../src/routes/+layout.svelte), [`command/`](../src/lib/components/ui/command/)                                                                                                                                                                                                                                                                           | `apiListClasses` / `apiListMediaTypes`                                                                                     |
| Breadcrumbs + deep links     | Shared **breadcrumb** trail (`Breadcrumbs` on the shadcn `breadcrumb` primitive) in the content header of the Files & Records hubs (`Home › <Sub-app> › <Entity>`), and **deep-linkable** open items via `?file=` (Files) / `?record=` (Records) so reload/share/back-forward restore the view (`replaceState`, restored on mount; never refetches on autosave). The final (entity) crumb shows the entity's icon (see **Per-entity icons**).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | stable | [`Breadcrumbs.svelte`](../src/lib/components/Breadcrumbs.svelte), [`files/+page.svelte`](../src/routes/files/+page.svelte), [`media/+page.svelte`](../src/routes/media/+page.svelte), [`breadcrumb/`](../src/lib/components/ui/breadcrumb/)                                                                                                                                                                                                         | —                                                                                                                          |
| Per-entity icons             | Each **class** and **record type** can carry a custom icon — a curated **Lucide** id (`core/icons.ts`, single source of truth for assignable ids; `EntityIcon.svelte` owns the id→component map + generic fallback). Chosen via the searchable **`IconPicker`** (avatar trigger beside Display name) in the **General tab** of the unified `EntitySettingsDialog`. Stored additively next to `displayName`: class `config.icon` / type `settings.json` `icon` (absent ⇒ generic fallback — `tag` for classes, `file-text` for types; **no migration**). Rendered on **both rails** (`RecordsRail` rows + collapsed; Files class filter rows + the collapsed-rail glyph, replacing the old 2-letter initials), the **breadcrumb** entity crumb, the **command palette** entries, and the Files **grid class chips** (`GridChip.icon`/`iconFallback`). Reserved entities (All Files, Globals) and sub-app icons stay fixed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | stable | [`icons.ts`](../src/lib/core/icons.ts), [`EntityIcon.svelte`](../src/lib/components/EntityIcon.svelte), [`IconPicker.svelte`](../src/lib/components/IconPicker.svelte), [`EntitySettingsDialog.svelte`](../src/lib/components/entity-settings/EntitySettingsDialog.svelte), [`adapters.ts`](../src/lib/components/entity-settings/adapters.ts)                                                                                                      | `PATCH /api/classes/[id]` (`icon`), `GET/POST /api/media-types/[typeId]/settings` (`icon`)                                 |

---

## HTTP API index (quick reference)

### Files / blobs (`/api/files/...`)

| Path pattern                                | Role                                                                                                                                           |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /api/files`                            | All Files hub list (`FileListResponse`; `?query&classIds&match=all\|any&unclassified&groupByClass&groupByField&searchField`; carries `healed`) |
| `POST /api/files/upload`                    | Multipart upload (any type; HEIC→JPEG); registers the blob, returns `id`                                                                       |
| `GET /api/files/[id]/blob`                  | Raw blob bytes                                                                                                                                 |
| `POST /api/files/[id]/rename`               | Rename blob on disk + manifest (O(1)); body `{ new_filename }`                                                                                 |
| `POST /api/files/delete`                    | Bulk delete from disk; body `{ ids }` (strips member records across classes)                                                                   |
| `GET /api/files/[id]/metadata`, `.../strip` | EXIF read / strip (`{ mode: 'all'\|'gps' }`)                                                                                                   |
| `GET /api/files/[id]/classes`               | Per-file editor sections: each class this blob belongs to + its schema/config/record (also the delete-usage surface)                           |
| `GET /api/files/missing`                    | Global missing-files warning: dangling refs grouped per absent blob                                                                            |

### Classes (`/api/classes/...`)

| Path pattern                                   | Role                                                                                               |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `GET/POST /api/classes`                        | List classes (+counts) / create (`{ displayName, schema? }`)                                       |
| `GET/PATCH/DELETE /api/classes/[id]`           | Read schema+config / update config (rename, grid prefs) / delete class                             |
| `GET/POST/DELETE /api/classes/[id]/members`    | Catalog view (`FileListResponse`, `?groupBy&query&searchField`) / add / remove members (`{ ids }`) |
| `/api/classes/[id]/schema`                     | Schema CRUD: `GET` read, `POST` add, `PATCH` update, `DELETE` remove, `PUT` replace                |
| `GET/PATCH /api/classes/[id]/records/[fileId]` | One member's record (carries `_missing_files`) / update fields                                     |
| `POST /api/classes/[id]/records/bulk-update`   | Apply one patch to many members (`{ ids, patch }`)                                                 |
| `GET /api/classes/[id]/field-values`           | Distinct values for autocomplete (`?field`)                                                        |

### `json` media types (`/api/media-types/...`)

| Path pattern                                                               | Role                                                                                                           |
| -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `GET/POST /api/media-types`                                                | List `json` types / create (`{ displayName }`)                                                                 |
| `/api/media-types/[typeId]`                                                | Get / patch display name / delete                                                                              |
| `.../stats`, `.../schema`, `.../settings`                                  | Counts+mtime / schema CRUD / type settings (display name + `displayField` title-by + `subtitleField` + `icon`) |
| `.../records/list`, `.../records`                                          | List (`JsonListResponse`; `?filters&groupBy&titleField&subtitleField&searchQuery&searchField`) / create record |
| `.../records/field-values`, `.../records/by-id/[id]`                       | Autocomplete / get·patch·delete record                                                                         |
| `.../records/bulk-update`, `.../records/bulk-delete`, `.../records/repair` | Bulk patch / bulk delete / schema repair                                                                       |
| `/api/media-types/globals/record`                                          | Read/update singleton globals object (`null` removes key)                                                      |
| `GET`/`POST /api/settings`                                                 | Global app prefs (grid size, nav, `railCollapsed`) in `media/settings.json`                                    |

Legacy `/api/images/*`, `/api/schema`, `/api/config` were **removed** — migrate via `upgrade-data`.

### Client wrappers

Files/classes: [`src/lib/api/files.ts`](../src/lib/api/files.ts). `json`-side `*ForType` helpers: [`src/lib/api/client.ts`](../src/lib/api/client.ts).

---

## Cross-cutting modules

| Area                      | Responsibility                                                                                                                                 | File(s)                                                   |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| Path resolution           | `media/` paths (files/manifest/classes/settings), `json` type paths, `listClassIds`                                                            | [`paths.ts`](../src/lib/storage/paths.ts)                 |
| Files + class persistence | Class CRUD, schema, opt-in membership, All Files + catalog listing, per-blob ops, missing-file scan                                            | [`classRepo.ts`](../src/lib/storage/classRepo.ts)         |
| Global blob manifest      | id registry + derived `classes[]` (`media/manifest.json`): mint/rename/remove, reconcile, membership index, dims                               | [`manifest.ts`](../src/lib/storage/manifest.ts)           |
| JSON-record persistence   | List, CRUD, schema for `json` kind                                                                                                             | [`jsonRepo.ts`](../src/lib/storage/jsonRepo.ts)           |
| Type settings on disk     | Read/write a `json` type's `settings.json` (display name, schema, data filename, `displayField` title-by, `subtitleField` subtitle-by, `icon`) | [`settingsFile.ts`](../src/lib/storage/settingsFile.ts)   |
| Global app settings       | Read/write app-wide prefs (grid size, navigation) in `media/settings.json`                                                                     | [`mediaSettings.ts`](../src/lib/storage/mediaSettings.ts) |
| Type CRUD                 | Create/delete/list `json` types, ensure globals                                                                                                | [`mediaTypes.ts`](../src/lib/storage/mediaTypes.ts)       |
| Server repo accessor      | Validates id, returns `JsonRepo`                                                                                                               | [`imageRepo.ts`](../src/lib/server/imageRepo.ts)          |
| Types + Zod               | Shared DTOs (incl. `ClassFile`, `FileItem`, `FileListResponse`)                                                                                | [`types.ts`](../src/lib/core/types.ts)                    |
| IDs                       | Record id + blob `file_id` (both UUID)                                                                                                         | [`ids.ts`](../src/lib/core/ids.ts)                        |
| CLI entry                 | Run built app with `MEDIA_MANAGER_ROOT`                                                                                                        | [`bin/media-manager.js`](../bin/media-manager.js)         |

---

## Residual notes

- **README** is the canonical place for install, **build + run** (`npm run build`, `node build`, `media-manager <root>`), and data layout examples.
- **`docs/FUTURE_CHANGES.md`** is the deferred-work backlog (non-committing ideas; do not treat it as the shipped feature set). `tasks.md` has been consolidated into it and is now just a pointer.
- **Data upgrade tool:** [`scripts/upgrade-data.mjs`](../scripts/upgrade-data.mjs) (`npm run upgrade-data -- <root>` or `... -- <root> --apply`) scans a data root and reports/applies the expected upgrades, culminating in the **file-first reorg** (step 5): relocate `files/` → `media/files/`, rebuild the manifest at `media/manifest.json` (v2, with derived `classes[]`), and convert each `images`/`generic` catalog folder → `media/classes/<id>.json` (`{ schema, config, records }`, dropping `image_name`/`excludedFiles`); the `blob_store` `files` type dissolves; `json`/`globals` folders stay. Idempotent (skipped once `media/classes` exists), dry-run by default.

---

## Roadmap / non-authoritative ideas

See [`FUTURE_CHANGES.md`](FUTURE_CHANGES.md). Items there are **not** commitments; this file describes what the codebase is meant to do today.
