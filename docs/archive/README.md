# Archived docs

Design plans and HTML mockups for features that have **shipped**. Kept for historical/design-rationale reference only — **not** current. The authoritative current state lives in [`../FEATURES.md`](../FEATURES.md) (what is) and [`../FUTURE_CHANGES.md`](../FUTURE_CHANGES.md) (what's next). Internal cross-links between these archived files may be stale.

| File                                                                            | Shipped feature                                                              |
| ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `entity-settings-plan.html` · `entity-settings-implementation.md`               | Unified per-entity `EntitySettingsDialog` (General / Fields / Danger)        |
| `globals-redesign-ideas.html` · `globals-redesign-plan.md`                      | Globals editor redesign (`GlobalsEditorPane`)                                |
| `records-ui-plan.html` · `RECORDS_EXPLORER.md`                                  | Records Explorer (`/media` three-pane)                                       |
| `records-grid-panel-plan.html`                                                  | Records grid + side-panel migration (shared `DataGrid` + `RecordDetailPane`) |
| `records-title-subtitle-plan.html` · `records-title-subtitle-implementation.md` | Title-by / subtitle-by (`displayField`/`subtitleField`)                      |
| `unify-files-records-plan.html` · `unify-files-records-implementation.md`       | Unified Files + Records interface (shared `DataGrid`/rails)                  |
| `ui-redesign.html`                                                              | Early dashboard / UI redesign                                                |
| `ICON_PICKER_WORKFLOW.html`                                                     | Per-entity icon picker (Item 28)                                             |

Safe to delete this whole folder if you don't want the history; git retains the tracked files regardless.
